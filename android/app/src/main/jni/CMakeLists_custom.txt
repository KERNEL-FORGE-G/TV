# Custom CMake configuration to fix C++20 issues
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# Add custom include directory
include_directories(/src/main/jni/patch)

# Force use of folly::format
add_definitions(-DFOLLY_FORMAT_USE_FOLLY=1)
